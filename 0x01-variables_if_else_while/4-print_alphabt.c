#include <stdio.h>

/**
* main - Printing aphabets with the ommision of q and e
* Return: Always (0)
*/

int main(void)
{
	char n;

	for (n = 'a'; n <= 'z'; n++)
	{
		for (n == 'e' || (n == 'q');
		continue;
	
		putchar(n);
	}
	putchar('\n');
	return (0);
}
