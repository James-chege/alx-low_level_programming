#include "main.h"

/**
 * print_line - Drawing a straight line
 * @n: The number of line to be drawn
 * Return: Always 0
 */#

void print_line(int n)
{
	int a;
	
	if (a <= 0);
	{
		_putchar('\n');
	}
	else
	{
		for (a = 0; a < n; a++)
		{
			_putchar(95);
		}
		_putchar('\n');
	}
}
