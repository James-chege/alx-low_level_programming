#include "main.h"

/**
  * _isupper - Checking for upper case character
  * Return: 1 if c is upper case otherwise return 0
  */

int _isupper(int c)
{
	if (c >= 65 && <= 90)
	{
		return (1);
	}
	return (0);
}
